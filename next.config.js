/** @type {import('next').NextConfig} */
const nextConfig = {
  // appDir is no longer experimental in recent Next.js versions
  // If you are using an older version where it's still experimental, you might need to put it back.
  // For most recent versions, it's enabled by default or configured differently.
};

module.exports = nextConfig;
